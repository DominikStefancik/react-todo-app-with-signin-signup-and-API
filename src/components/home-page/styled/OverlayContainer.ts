import styled from 'styled-components';
import { LayoutProps } from '../../types';

export const OverlayContainer = styled.div<LayoutProps>`
  position: absolute;
  top: 0;
  left: 50%;
  width: 50%;
  height: 100%;
  overflow: hidden;
  transition: transform 0.6s ease-in-out;
  z-index: 100;
  ${(props) => (props.isSignInForm !== true ? `transform: translateX(-100%);` : null)}
`;

export const Overlay = styled.div<LayoutProps>`
  background: #ff416c;
  background: -webkit-linear-gradient(to right, #ff4b2b, #ff416c);
  background: linear-gradient(to right, #ff4b2b, #ff416c);
  background-repeat: no-repeat;
  background-size: cover;
  background-position: 0 0;
  color: #ffffff;
  position: relative;
  left: -100%;
  height: 100%;
  width: 200%;
  transform: translateX(0);
  transition: transform 0.6s ease-in-out;
  ${(props) => (props.isSignInForm !== true ? `transform: translateX(50%);` : null)}
`;
